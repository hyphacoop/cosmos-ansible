---
all:
  vars:
    ansible_user: root
    # Global variables for all nodes
    chain_id: "perf-testnet"
    pyroscope_server_address: "http://159.203.3.222:4040/"
    remote_write_url: "http://159.203.3.222:9091/api/v1/write"

    # Monitoring server
    grafana_ssh_url: root@monitor.polypore.xyz

    # Enable prometheus configuration
    monitoring_prometheus: true

    # HTTPS exporter targets
    prometheus_https_exporters:
      - name: factory-monitor
        target: factory-monitor.polypore.xyz
        service: token-factory
        environment: polypore
        metrics_path: /
  children:
    node:
      hosts:
        dummy.earthball.xyz:
