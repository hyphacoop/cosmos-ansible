---
# yamllint disable rule:line-length
all:
  vars:
    gaiad_home_autoclear: false
    gaiad_version: v7.0.1
    ansible_user: root
  children:
    gaia:
      hosts:
        "{{ target }}":
          fast_sync: false
          chain_id: my-testnet
